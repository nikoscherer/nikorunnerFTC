## Requirements
`1` Create Hermite Cubic Splines
`2` Get coordinates along the splines
`3` Go to the coordinate using Vectors
`4` Use Odometry to localize
`6` Allow things to run at specific points along the path
`5` Make pathing asynchronous